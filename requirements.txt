# InstitutionScanner — Python dependencies
# Install: pip install -r requirements.txt

# Core
pandas>=2.0.0,<3.0
numpy>=1.24.0,<3.0
scipy>=1.10.0

# Market data / fundamentals
# TickFlow Free is the sole OHLCV + universe provider.
tickflow[all]==0.1.24
# AkShare is retained only for low-frequency fundamental refreshes.
akshare==1.16.53

# Technical indicators
ta>=0.11.0

# Parallelism & progress
tqdm>=4.65.0

# Export
pyarrow>=12.0.0

# Optional: plotting (used in future dashboard module)
matplotlib>=3.7.0
